{
  "name": "backend",
  "version": "1.0.0",
  "description": "Servidor JSON para Render",
  "private": true,
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.1",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.x"
  }
}
