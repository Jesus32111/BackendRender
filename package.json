{
  "name": "backend",
  "version": "1.0.0",
  "description": "Servidor JSON para Render (usando server.js)",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.1",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.x"
  }
}
