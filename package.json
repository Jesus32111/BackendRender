{
  "name": "backend",
  "version": "1.0.0",
  "description": "Servidor JSON para el proyecto (sin index.js)",
  "private": true,
  "scripts": {
    "start": "json-server db.json --host 0.0.0.0 --port $PORT --no-static"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.1",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.x"
  }
}
